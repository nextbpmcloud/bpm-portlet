// Requires jQuery
// Force poll every X seconds to keep alive session
if (jQuery !== undefined) {

    //var timerfunc = function(){
    //    jQuery.get('/bpm-portlet/bpmportlet/');
    //};
    //jQuery(function(){
    //    window.setInterval(timerfunc, 60000);
    //});
}

